<Window
    x:Class="WPF.Reflex.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF.Reflex"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WPF.Reflex.ViewModels"
    Title="MainWindow"
    Width="800"
    Height="450"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="MenuItem" TargetType="MenuItem">
            <Setter Property="Height" Value="30" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="16" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Menu
            Height="30"
            VerticalAlignment="Center"
            FontSize="16">
            <MenuItem Header="DLL配置" Style="{StaticResource MenuItem}">
                <MenuItem
                    Command="{Binding RegisterDllCommand}"
                    Header="注册DLL"
                    Style="{StaticResource MenuItem}" />
                <MenuItem Header="重新加载" Style="{StaticResource MenuItem}" />
            </MenuItem>
        </Menu>
    </Grid>
</Window>
